O[O# .dependabot/config.yml

version: 2
updates:
  - package-ecosystem: "npm"        # Specify the package manager you want to monitor (e.g., npm, pip, bundler, maven).
    directory: "/"                  # Specify the directory where your package manager files are located.
    schedule:
      interval: "daily"             # Set the update frequency (e.g., daily, weekly).
    open-pull-requests-limit: 5      # Limit the number of open pull requests generated by Dependabot.

  # You can add more package manager configurations here if needed.

